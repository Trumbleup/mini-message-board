extends layout

block content
  h1= title
  ul
     each message in messages
        li= message.user
        li= message.text
        li= message.added
  a(class='button' href='//localhost:5000/new') Submit New Message

